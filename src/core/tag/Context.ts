/**
 * コンテキストインターフェース
 * クラス図P2_Context.Contextに対応
 * 
 * 全てのコンテキスト型の基底インターフェース
 * 各レイヤーで異なるコンテキスト型を定義する際に継承する
 */
export interface Context {
  // 基底インターフェースとして、共通のメソッドやプロパティは現時点では定義しない
  // 各実装クラスで具体的な構造を定義
}